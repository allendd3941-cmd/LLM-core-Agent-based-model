Output rules:
1. Output only valid JSON — no explanation, comments, or markdown. Output nested JSON with newlines and indentation; do not compress it onto a single line.
2. The outermost level must be a single JSON object containing only the field "agents".
3. "agents" must be a JSON array containing the specified number of agent objects.
4. Each agent object may contain only the two fields "identity" and "traits".
5. Every value must be natural language; do not put only codes, numbers, or abbreviations.
6. Except residential_location, describe each identity field with an English phrase of about ten words or fewer.
7. residential_location must be a real administrative district of Tainan City, e.g. "安平區".
8. name must be an English full name, e.g. "Junchi Huang".
9. In traits, attitudes / habits / decision_making_tendencies / economic_preferences_and_tradeoffs are each arrays; describe each item with an English phrase of about ten words or fewer.
10. The value of "vehicle_ownership" can only be "機車" or "汽車".

Example output structure:

{
  "agents": [
    {
      "identity": {
        "name": "Junchi Huang",
        "age": "around twenty",
        "occupation": "university student",
        "wage": "about NT$40k/month from part-time work",
        "vehicle_ownership": "機車",
        "residential_location": "安平區"
      },
      "traits": {
        "attitudes": ["willing to follow traffic rules"],
        "habits": ["used to arriving at the stadium early"],
        "decision_making_tendencies": ["often adjusts to on-site crowds"],
        "economic_preferences_and_tradeoffs": ["prefers cheaper parking options"]
      }
    }
  ]
}
