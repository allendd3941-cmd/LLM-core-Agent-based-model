Based on the "environment information" and "agent travel memory", together with the "agent profile data", produce a decision-making result for each agent. Consider each agent's personality, environmental perception, and travel memory when deciding.

There are three action modes below. Based on each agent's nature, travel memory, and the environment information, fill in the action mode value with one of:
"fast"                 // wants to go faster
"tolerate_congestion"  // fine to stay in congestion, no detour
"avoid_congestion"     // avoid congestion, reroute around it

When choosing the action mode, pay special attention to the following cues in the environment information (combined with the agent's personality):
- `road_ahead`: if it shows congestion ahead, lean toward "avoid_congestion" (unless the agent's personality is fine with congestion, in which case "tolerate_congestion").
- `traffic_here` / `speed_status`: already congested underfoot or crawling, and still jammed ahead → lean toward avoiding, or (for those who dislike detours) tolerating.
- `congestion_hotspots` / `congestion_trend`: if the agent's current or upcoming district is a hotspot, or city-wide congestion is worsening, "avoid_congestion" fits better.
- if the road ahead is clear and overall congestion is light, by personality choose "fast" (in a hurry) or "tolerate_congestion" (cruising).

agent name is each agent's name; generate it from the "name" value in the "agent profile data", do not omit any.
vehicle_type is generated from the "vehicle_ownership" value in the "agent profile data"; do not omit any, and it can only be one of two values: "motorcycle" or "car".
reason is why the agent chose this action mode; explain concisely in English (about 10–20 words), combining personality, travel memory, and environment (e.g. "Congested ahead and in a hurry, so taking a detour", or "Safety-minded, so no rerouting even when congested" — not limited to these).
The decision-making output must match the structure below exactly; do not produce any text other than this structure:
{
  "agents": [
    {
      "agent name": "David Wang",
      "action mode": "fast",
      "vehicle_type":"motorcycle",
      "reason": "Roads are clear and in a hurry, wants to arrive quickly"
    }
  ]
}

Environment information:
