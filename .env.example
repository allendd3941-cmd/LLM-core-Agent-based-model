# LLM 連線設定（vLLM）。
# 註：VLLM_MODEL 可由前端「LLM 模型」下拉於 runtime 覆寫（＝對齊目標，整套 LLM 用途共用）。
# 先在 Linux/GPU 啟動 vLLM（見 docs/SCALING_zh-TW.md）：
#   vllm serve <HF模型> --port 8001 --max-model-len 8192 --enable-prefix-caching

# vLLM server URL
VLLM_URL=http://127.0.0.1:8001
# 對齊的 HF 模型名（必填；須與 `vllm serve` 啟動的模型一致，否則 LLM 呼叫會用空模型名失敗）
VLLM_MODEL=Qwen/Qwen2.5-7B-Instruct
